import { __rest } from "tslib";
import React from 'react';
import classnames from 'classnames';
import { BaseField, filterDOMProps, nothing } from 'uniforms';
const ErrorsField = (_a, { uniforms: { error, schema } }) => {
    var { className, children } = _a, props = __rest(_a, ["className", "children"]);
    return !error && !children ? (nothing) : (React.createElement("div", Object.assign({ className: classnames('panel panel-danger', className) }, filterDOMProps(props)),
        React.createElement("div", { className: "panel-body" },
            children,
            schema.getErrorMessages(error).map((message, index) => (React.createElement("div", { key: index }, message))))));
};
ErrorsField.contextTypes = BaseField.contextTypes;
export default ErrorsField;
